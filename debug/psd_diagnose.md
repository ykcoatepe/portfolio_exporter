PSD’s zero P&L rows trace back to ingestion coercing missing cost basis values to `0.0`. `libs/py/positions_engine/ingest/internal.py:575-589` writes `avg_cost` as `0.0` whenever a snapshot omits the field, so downstream `Position` objects never see `None`. The guard in `libs/py/positions_engine/service/normalize.py:315-332` then treats `avg_cost == 0` as “no basis” and short-circuits the total P&L calculation. The reproduction snippet in `debug/psd_logs.txt` confirms `compute_equity_pnl_fields` returns `total_pnl => None` once `avg_cost` is forced to zero.

Option legs inherit a hard 24h staleness badge because quote normalization remaps `LAST_CLOSE → PREV` without preserving timestamps. `_MARK_SOURCE_ALIASES` in `libs/py/positions_engine/service/state.py:1185-1203` aliases `LAST_CLOSE` to `PREV`, yet `_option_leg_record` never populates `previous_close_ts`. `_resolve_option_stale_seconds_entry` (lines 1553-1582) therefore falls back to `_PREV_STALE_FALLBACK_SECONDS == 86400`. The failing regression in `debug/psd_logs.txt` (`test_leg_missing_timestamps_uses_fallback_staleness`) shows the UI payload surfacing `stale_seconds == 60`, validating the mismatch between equities, where `_derive_single_stock_rows` seeds `stale_s = 0` (lines 196-214), and options, which default to a 24h badge.

Option combos render empty because leg marks are never rebuilt when quotes arrive without bid/ask/last fields. `_option_leg_record` (lines 604-650) skips emitting a quote when all three are `None`. `build_option_leg_snapshot` (libs/py/positions_engine/combos/detector.py:214-240) then calls `select_equity_mark` with `quote=None`, producing `mark=None` and cascading `total_pnl=None`. The Vitest failure in `debug/psd_logs.txt` (`OptionLegsTable > applies delta range filter`) shows the UI filtering against rows with empty marks, mirroring the missing combo data.

Together these behaviors explain PSD’s empty P&L cells and chronic 24h stale badges. Restoring `avg_cost=None`, preserving previous-close timestamps, and synthesizing fallback marks for quote-less legs all unblock accurate dashboard metrics.
