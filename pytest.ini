[pytest]
addopts = -q --durations=15
pythonpath =
    libs/py
markers =
    integration: API/ASGI integration-style tests (may be slower)
    slow: long-running tests (exclude for fast local loop)
    cli: spawns subprocess/CLI invocations
