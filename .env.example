# PSD pluggable hooks
PSD_SNAPSHOT_FN=portfolio_exporter.psd_adapter:snapshot_once
PSD_RULES_FN=portfolio_exporter.psd_rules:evaluate
# Optional: heartbeat interval for ingestor loop
PSD_HEARTBEAT_S=2.0
# Leave unset in production; test harnesses toggle this to simulate SSE.
#PSD_SSE_TEST_MODE=1
# SQLite WAL checkpoint tuning
PSD_WAL_AUTOCHECKPOINT=1000
PSD_CHECKPOINT_EVERY=300
CP_REFRESH_TOKEN=""  # Optional: IBKR Client Portal refresh token

# ─────────────────────────────────────────────────────────────
# Powerlaw (TRADER v5) integration for PSD
# ─────────────────────────────────────────────────────────────
# Powerlaw repo root; defaults to ../codeforge-powerlaw-trader if present.
#PSD_POWERLAW_REPO=/absolute/path/to/codeforge-powerlaw-trader
# Powerlaw output directory (relative to repo root if not absolute)
#PSD_POWERLAW_OUTPUT_DIR=output
# Auto-refresh when stale (1 trading day old)
#PSD_POWERLAW_REFRESH=1
#PSD_POWERLAW_STALE_TRADING_DAYS=1
# Refresh command override (optional)
#PSD_POWERLAW_CMD="python scripts/run_trader_v5_daily.py --refresh-prices --refresh-vx --no-input --no-orders --allow-warn --output-dir output"
# Refresh timeout/cooldown (seconds)
#PSD_POWERLAW_REFRESH_TIMEOUT_SEC=300
#PSD_POWERLAW_REFRESH_COOLDOWN_SEC=900

# ─────────────────────────────────────────────────────────────
# IBKR Connection (Gateway-first with TWS fallback)
# ─────────────────────────────────────────────────────────────
# If IB_PORT is UNSET, scripts auto-try Gateway (4001) then TWS (7496).
# Set IB_PORT explicitly to disable fallback or for paper trading.
# ─────────────────────────────────────────────────────────────
IB_HOST=127.0.0.1
# Leave unset for auto-fallback, or set explicitly:
#IB_PORT=4001    # Gateway live (default when unset)
#IB_PORT=4002    # Gateway paper
#IB_PORT=7496    # TWS live (legacy/fallback)
#IB_PORT=7497    # TWS paper
# Use a unique client id per tool/session
IB_CLIENT_ID=1234

# Default output directory for generated reports (optional)
# If set, scripts like daily_report.py will write files here by default.
# Example: OUTPUT_DIR=./.outputs
#OUTPUT_DIR=

# Legacy override used by some scripts (e.g., net_liq_history_export)
#PE_OUTPUT_DIR=

# Quiet/CI mode: suppress pretty printing
#PE_QUIET=1

# TWS export directory for Net-Liq (dailyNetLiq.csv)
#TWS_EXPORT_DIR=~/Jts/Export
